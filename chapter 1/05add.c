#include <stdio.h>
int main()
{
    // this is s program to add two given numbers
    int a, b;
    printf("enter the value of a\n ");
    scanf("%d", &a);

    printf("enter the value of b\n ");
    scanf("%d", &b);

    printf("addition of a and b is \n %d", a + b);
    return 0;
}